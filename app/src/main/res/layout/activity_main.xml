<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.meshsami27.myrecipes.MainActivity">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:id="@+id/imageView"
        android:src="@drawable/main2"
        android:scaleType="centerCrop" />

    <TextView
        android:id="@+id/appNameTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="MyRecipes"
        android:textColor="@color/colorTextIcons"
        android:textSize="60sp"
        android:textStyle="bold"/>

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/locationTextView"
        android:hint="Enter Your Category"
        android:padding="10dp"
        android:layout_marginBottom="10dp"
        android:layout_above="@+id/findCategoriesButton">

    </EditText>

    <Button
        android:id="@+id/findCategoriesButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:background="@color/colorAccent"
        android:contentDescription="activity_main"
        android:text="Find Categories"
        android:textColor="@android:color/background_light"
        android:visibility="visible"
        tools:clickable="true"
        tools:cursorVisible="true" />






</RelativeLayout>